'use strict';

// Задача  8
// Напишите функцию, которая принимает цену товара price и возврадает новую цену с скидкой 3.5 %

let price = 1000;
let proc = 3.5;
console.log(`Итоговая цена со скидкой 3.5 % = ${(price / 100) * proc}`);
